{
  "name": "outstation-guru",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "turbo build",
    "dev:api": "firebase emulators:start --only auth,firestore,functions --project demo-outstation-guru",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.1.1",
    "typescript": "^5.5.4",
    "firebase-tools": "^13.0.4"
  }
}